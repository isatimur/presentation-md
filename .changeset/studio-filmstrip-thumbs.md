---
"@presentation-md/studio": minor
---

Studio SlideList **filmstrip thumbnails** — live scaled slide previews beside each row (lazy IntersectionObserver iframes via restyleSlideHtml), so navigation matches frontend-slides visual strip UX.
