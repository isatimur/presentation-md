---
"@presentation-md/render": patch
"@presentation-md/mcp-server": patch
---

Bound captured PDF, deploy, and live judge-panel subprocess output, and terminate process trees that exceed the configured byte limit before they can exhaust CLI or MCP memory. Live judge panels now also support a configurable hard timeout.
