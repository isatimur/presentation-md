---
"@presentation-md/core": minor
"@presentation-md/mcp-server": patch
"@presentation-md/render": minor
"@presentation-md/studio": patch
---

CLI `--generate-prompt` + Present digit jump / blackout e2e.

Extract shared `buildGenerateDeckPrompt` into core (MCP + CLI), wire CLI one-shot craft prompt export, and Present 1–9 / Home / End jump with blackout + timer coverage.
