---
"@presentation-md/core": minor
"@presentation-md/mcp-server": minor
"@presentation-md/render": minor
"@presentation-md/studio": minor
---

Non-LLM structural density remorph (`remorphDensity`) + Present blackout/timer + CLI `--share-link`.

`audit_deck remorph_density` / CLI `--remorph-density` / Studio Speaker·Reading density buttons split crowded lists or promote notes; Present gains B blackout + T elapsed timer; CLI `--share-link` prints Studio `?d=` URLs.
