---
"@presentation-md/render": patch
"@presentation-md/mcp-server": patch
---

Share canonical title/layouts theme-preview decks between CLI `--preview-compare` and MCP `preview_themes` (incl. kinetic-wrapped craft).
