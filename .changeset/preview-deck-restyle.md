---
"@presentation-md/mcp-server": minor
"@presentation-md/core": patch
---

MCP `preview_themes` accepts Deck JSON (`json` / mode `deck`) for Studio My deck restyle parity — content-true theme compares with optional `slide_index` PNGs.
