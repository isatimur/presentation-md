---
"@presentation-md/core": patch
"@presentation-md/mcp-server": patch
---

Require asymmetric craft and speaker notes in agent authoring docs (deck-schema + SKILL) so MCP `generate_deck_prompt` matches Studio generate rules.
