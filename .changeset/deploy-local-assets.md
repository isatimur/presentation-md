---
"@presentation-md/core": patch
---

Reject single-file deploys that reference local assets through quoted or unquoted attributes, responsive image fields, poster fields, CSS URLs, or quoted CSS imports; bound asset inspection to 64 MiB; validate local input before invoking the Vercel CLI; and reject ambiguous multiple deploy targets.
