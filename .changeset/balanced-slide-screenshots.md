---
"@presentation-md/render": patch
---

Preserve complete top-level slide outer HTML when screenshot isolation encounters nested section, div, or `.slide` elements, and emit a matching closing tag for custom deck surfaces.
