---
"@presentation-md/core": minor
"@presentation-md/render": minor
"@presentation-md/mcp-server": patch
"@presentation-md/studio": minor
---

CLI `--list-themes` discovery parity (browse / studio_url / suggested_preview) + Present pace countdown (P).

Shared `buildThemesDiscoveryList` powers MCP `list_themes` and CLI `--list-themes` / `--browse` / `--json`. Studio + exported HTML Present gain a P-cycled 5–30m pace timer with ahead/behind/over cues.
