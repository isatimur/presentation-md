---
"@presentation-md/studio": minor
---

Studio **Paste Brand** panel: paste `:root` CSS → ephemeral session theme (contrast-safe extract + Apply with repairCraft) — MCP `import_brand_theme` / `--from-css` parity without scaffolding a package.
