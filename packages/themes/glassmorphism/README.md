# glassmorphism

**Soft glassmorphism — icy lavender field, indigo + cyan accents, Plus Jakarta Sans.**

```bash
npm i @presentation-md/theme-glassmorphism
```

Use: `"meta": { "theme": "glassmorphism" }` · Surface: `glass-mist`.
