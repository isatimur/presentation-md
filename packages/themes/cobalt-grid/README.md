# cobalt-grid

**Graph-paper cobalt with classical serif display.**

```bash
npm i @presentation-md/theme-cobalt-grid
```

Use: `"meta": { "theme": "cobalt-grid" }` · Surface: `cobalt-grid-paper`.

Surface craft: cobalt graph-paper grid, diagonal corner hatch, quiet cobalt frame. PPTX mirrors denser grid + hatch stubs + outer frame + square cobalt card strokes.

Faithful to frontend-slides `bold-template-pack cobalt-grid`.
