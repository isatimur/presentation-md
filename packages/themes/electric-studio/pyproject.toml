[project]
name = "presentation-md-theme-electric-studio"
version = "1.0.0"
description = "Electric Studio — split white/blue panels, Manrope, accent bar."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Timur Isachenko" }]
keywords = ["presentation-md", "theme", "ai-agent", "slides"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/presentation_md_theme_electric_studio"]

[tool.hatch.build.targets.wheel.force-include]
"theme.json" = "presentation_md_theme_electric_studio/theme.json"
