[project]
name = "presentation-md-theme-corporate"
version = "0.1.0"
description = "Formal corporate presentation theme — crisp white, navy text, restrained blue accent."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Timur Isachenko" }]
keywords = ["presentation-md", "theme", "ai-agent", "slides"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/presentation_md_theme_corporate"]

[tool.hatch.build.targets.wheel.force-include]
"theme.json" = "presentation_md_theme_corporate/theme.json"
