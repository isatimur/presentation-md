# y2k-aero

**Y2K aero — icy gradients, chrome cyan, soft bubbles, futuristic optimism.**

```bash
npm i @presentation-md/theme-y2k-aero
```

Use: `"meta": { "theme": "y2k-aero" }` · Surface: `aero-bubble`.
