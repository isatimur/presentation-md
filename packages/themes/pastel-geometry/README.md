# pastel-geometry

**White card on pastel with vertical pills.**

```bash
npm i @presentation-md/theme-pastel-geometry
```

Use: `"meta": { "theme": "pastel-geometry" }` · Surface: `pastel-geometry-pills`.

Faithful to frontend-slides `STYLE_PRESETS Pastel Geometry`.

Surface craft: outer sky matte ring + soft shadow + vertical pastel edge pills (HTML). PPTX mirrors matte/shadow/pills + plump card radius.