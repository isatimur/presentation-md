# signal

**Economist restraint × private briefing — cream and navy linked by antique gold.**

```bash
npm i @presentation-md/theme-signal
```

Use: `"meta": { "theme": "signal" }` · Surface: `signal-briefing`.

Faithful to frontend-slides / beautiful-html-templates `signal`.
