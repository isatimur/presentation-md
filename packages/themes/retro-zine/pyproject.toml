[project]
name = "presentation-md-theme-retro-zine"
version = "1.0.0"
description = "Beige paper with green accent and Bebas Neue + Caveat: riso-printed zine."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Timur Isachenko" }]
keywords = ["presentation-md", "theme", "ai-agent", "slides"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/presentation_md_theme_retro_zine"]

[tool.hatch.build.targets.wheel.force-include]
"theme.json" = "presentation_md_theme_retro_zine/theme.json"
