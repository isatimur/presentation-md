[project]
name = "presentation-md-theme-playful"
version = "1.0.0"
description = "Playful creative-agency presentation theme — warm white, coral + lime accents, rounded, big type."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Timur Isachenko" }]
keywords = ["presentation-md", "theme", "ai-agent", "slides"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/presentation_md_theme_playful"]

[tool.hatch.build.targets.wheel.force-include]
"theme.json" = "presentation_md_theme_playful/theme.json"
