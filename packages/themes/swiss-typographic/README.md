# swiss-typographic

**Swiss International Typographic Style — white grid, signal red, Helvetica-like grotesk.**

Install (npm):

```bash
npm i @presentation-md/theme-swiss-typographic
```

Use in deck JSON: `"meta": { "theme": "swiss-typographic" }`

Surface profile: `swiss-grid`.
