# grove

**Forest-green monograph with Playfair serifs and rust accent.**

```bash
npm i @presentation-md/theme-grove
```

Use: `"meta": { "theme": "grove" }` · Surface: `grove-monograph`.

Faithful to frontend-slides `bold-template-pack grove`.
