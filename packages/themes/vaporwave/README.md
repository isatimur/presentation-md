# vaporwave

**Vaporwave — purple dusk, sunset gradient, chrome teal, nostalgic mall energy.**

```bash
npm i @presentation-md/theme-vaporwave
```

Use: `"meta": { "theme": "vaporwave" }` · Surface: `vapor-horizon`.

Surface craft: dusk gradient, perspective grid, pink sun, and horizon glow line (HTML). PPTX mirrors wash/sun/grid/horizon glow + accent rim.