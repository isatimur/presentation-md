# ft-editorial

**Financial Times–inspired broadsheet — warm paper, ink, FT blue + signal red.**

Install (npm):

```bash
npm i @presentation-md/theme-ft-editorial
```

Use in deck JSON: `"meta": { "theme": "ft-editorial" }`

Surface profile: `broadsheet-rule`.
