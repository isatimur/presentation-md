[project]
name = "presentation-md-theme-bold-poster"
version = "1.0.0"
description = "Bold Poster — Shrikhand display, Libre Baskerville body, tomato red accent."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "Timur Isachenko" }]
keywords = ["presentation-md", "theme", "ai-agent", "slides"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/presentation_md_theme_bold_poster"]

[tool.hatch.build.targets.wheel.force-include]
"theme.json" = "presentation_md_theme_bold_poster/theme.json"
