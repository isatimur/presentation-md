# pin-and-paper

**Yellow legal pad pinned with cobalt ink — field notebook energy.**

```bash
npm i @presentation-md/theme-pin-and-paper
```

Use: `"meta": { "theme": "pin-and-paper" }` · Surface: `pin-paper-pad`.

Faithful to frontend-slides / beautiful-html-templates `pin-and-paper`.

Surface craft: legal-pad rules, red margin, pin, and a quiet paper tooth under the lines (HTML). PPTX approximates pad chrome — grain is HTML-only.
