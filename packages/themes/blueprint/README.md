# blueprint

**Engineering blueprint — deep navy, cyan lines, Space Mono / Space Grotesk.**

```bash
npm i @presentation-md/theme-blueprint
```

Use: `"meta": { "theme": "blueprint" }` · Surface: `blueprint-grid`.
