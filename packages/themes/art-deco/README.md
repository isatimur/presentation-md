# art-deco

**Art Deco investor — deep emerald, gold leaf, Cinzel display.**

```bash
npm i @presentation-md/theme-art-deco
```

Use: `"meta": { "theme": "art-deco" }` · Surface: `deco-fan`.

Surface craft: gold frame + top hairline on every slide; deco ring on heroes (HTML). PPTX densifies frame/hairline + dual hero rings + square gold card strokes.
