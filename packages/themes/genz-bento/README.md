# genz-bento

**Gen-Z hard-shadow bento — hot coral, lime stickers, chunky ink borders.**

Install (npm):

```bash
npm i @presentation-md/theme-genz-bento
```

Use in deck JSON: `"meta": { "theme": "genz-bento" }`

Surface profile: `hard-bento`.
