# crt-terminal

**CRT phosphor terminal — near-black, acid green glow, cyan accents, monospace.**

Install (npm):

```bash
npm i @presentation-md/theme-crt-terminal
```

Use in deck JSON: `"meta": { "theme": "crt-terminal" }`

Surface profile: `crt-phosphor`.
