# bauhaus

**Bauhaus primary system — cream field, red/yellow/blue geometry, bold grotesk.**

```bash
npm i @presentation-md/theme-bauhaus
```

Use: `"meta": { "theme": "bauhaus" }` · Surface: `bauhaus-blocks`.

Surface craft: hard frame + red square + blue circle on every slide (HTML). PPTX mirrors frame + always-on geometry (heroes larger) + square hard-border cards.
