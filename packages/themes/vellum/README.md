# vellum

**Gallery wall colorfield — one periwinkle, chartreuse italic serif.**

```bash
npm i @presentation-md/theme-vellum
```

Use: `"meta": { "theme": "vellum" }` · Surface: `vellum-colorfield`.

Faithful to frontend-slides / beautiful-html-templates `vellum`.

Surface craft: soft colorfield wash plus a quiet vellum fiber grain on every slide (HTML). PPTX approximates the wash — grain is HTML-only.
