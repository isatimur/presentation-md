# neo-grid-bold

**Magazine block grid — ecru, ink, electric lemon panels.**

```bash
npm i @presentation-md/theme-neo-grid-bold
```

Use: `"meta": { "theme": "neo-grid-bold" }` · Surface: `neo-grid-panels`.

Faithful to frontend-slides / beautiful-html-templates `neo-grid-bold`.
