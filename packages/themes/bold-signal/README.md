# bold-signal

**Confident high-impact — orange card on dark charcoal.**

```bash
npm i @presentation-md/theme-bold-signal
```

Use: `"meta": { "theme": "bold-signal" }` · Surface: `bold-signal-card`.

Faithful to frontend-slides / beautiful-html-templates `bold-signal`.

Surface craft: soft shadow + rounded charcoal frame + orange blot; orange focal panel on heroes (HTML). PPTX mirrors shadow/frame/blot + hero panel + plump card radius.