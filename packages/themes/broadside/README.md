# broadside

**Ink-on-fire protest poster — dark canvas, single fire-orange blast.**

```bash
npm i @presentation-md/theme-broadside
```

Use: `"meta": { "theme": "broadside" }` · Surface: `broadside-fire`.

Faithful to frontend-slides / beautiful-html-templates `broadside`.
