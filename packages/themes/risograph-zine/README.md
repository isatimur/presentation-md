# risograph-zine

**Risograph zine — warm paper, misregistered ink, magenta + teal print shop energy.**

```bash
npm i @presentation-md/theme-risograph-zine
```

Use: `"meta": { "theme": "risograph-zine" }` · Surface: `riso-print`.

Gallery-parity: coral + blue overprint washes (`mix-blend-mode: multiply` in HTML;
layered translucent ovals in PPTX).
