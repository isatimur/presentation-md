# biennale-yellow

**Solar yellow on parchment with indigo serif.**

```bash
npm i @presentation-md/theme-biennale-yellow
```

Use: `"meta": { "theme": "biennale-yellow" }` · Surface: `biennale-yellow-sun`.

Surface craft: solar yellow sun wash, quiet indigo frame, top hairline, coral stub. PPTX mirrors denser sun orbs + indigo frame + hairline/stub; cards get an indigo top accent rule (top-rule peer of quiet-luxe / ft-editorial — no full box stroke).

Faithful to frontend-slides `bold-template-pack biennale-yellow`.
