# biennale-yellow

**Solar yellow on parchment with indigo serif.**

```bash
npm i @presentation-md/theme-biennale-yellow
```

Use: `"meta": { "theme": "biennale-yellow" }` · Surface: `biennale-yellow-sun`.

Surface craft: solar yellow sun wash, quiet indigo frame, top hairline, coral stub. PPTX mirrors denser sun orbs + indigo frame + hairline/stub + square indigo card strokes.

Faithful to frontend-slides `bold-template-pack biennale-yellow`.
