# coral

**Magazine-poster coral planes with Bebas Neue caps.**

```bash
npm i @presentation-md/theme-coral
```

Use: `"meta": { "theme": "coral" }` · Surface: `coral-hatch`.

Faithful to frontend-slides `bold-template-pack coral`.

Surface craft: hard ink hatch frame + coral left wash on every slide (HTML). PPTX mirrors hatch/frame/wash + square hard-border cards.