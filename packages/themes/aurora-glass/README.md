# aurora-glass

**Dark aurora glassmorphism — void canvas, frosted cards, violet + cyan glow.**

Install (npm):

```bash
npm i @presentation-md/theme-aurora-glass
```

Use in deck JSON: `"meta": { "theme": "aurora-glass" }`

Surface profile: `aurora-glass`.
